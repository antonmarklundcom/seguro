NODE_ENV=development
DATABASE_URL=postgresql://seguro:seguro@localhost:5432/seguro
REDIS_URL=redis://localhost:6379
WORKER_CONCURRENCY=5

# Optional: without RESEND_API_KEY, email delivery logs to console instead
# of sending (safe for local dev before a Resend account exists).
RESEND_API_KEY=
RESEND_FROM_EMAIL=leads@seguro.com.py
