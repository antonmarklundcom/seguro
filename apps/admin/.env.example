DATABASE_URL=postgresql://seguro:seguro@localhost:5432/seguro
REDIS_URL=redis://localhost:6379

# Shared-password auth (docs/05) — replace with real SSO before multiple
# admin users need distinct audit trails.
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=change-me-too-a-long-random-string
